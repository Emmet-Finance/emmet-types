{
  "name": "@emmet-contracts/web3",
  "version": "0.1.0",
  "main": "dist/index.js",
  "license": "MIT",
  "scripts": {
    "copy-dts": "copyfiles -u 1 \"src/**/*.d.ts\" dist",
    "build": "tsc -p tsconfig.json && yarn copy-dts"
  },
  "dependencies": {
    "ethers": "^6.12.1"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "copyfiles": "^2"
  }
}